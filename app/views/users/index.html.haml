/ Page Heading
.d-sm-flex.align-items-center.justify-content-between.mb-4
  %h1.h3.mb-0.text-gray-800 អ្នកប្រើប្រាស់

  %a#btn-download.d-sm-inline-block.btn.btn-sm.btn-primary.shadow-sm{ href: download_users_path, target: :_blank }
    %i.fas.fa-download.fa-sm.text-white-50
    ទាញយកលទ្ធផល

= render 'filter'

.card.shadow.mb-4
  .card-body
    = render "shared/pagination_title", objects: @users

    %table.table
      %thead
        %tr
          %th{width: 60} #
          %th= t('user.student')
          %th= t('user.gender')
          %th= t('user.grade')
          %th= t('user.study_at')
          %th ការធ្វើតេស្ត(v1)
          %th= t('user.holland_quiz') + "v2"

      %tbody#body-content
        = render partial: 'users'

    = render "shared/footers/pagination"
