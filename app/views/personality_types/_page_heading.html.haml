= content_for :title do
  = t('sidebar.personality_type')

.d-flex.flex-row-reverse.flex-wrap
  .d-flex
    .mr-2= render "shared/buttons/download_button", url: personality_types_path(filter_params.merge(format: :xlsx))

    - if policy(PersonalityType).create?
      = render "shared/buttons/add_new_button", url: new_personality_type_path

      / More Options
      = render "shared/buttons/more_button" do
        = render "shared/buttons/import_from_file_button", url: new_personality_type_importer_path

        = render "shared/buttons/importing_history_button", name: t('sidebar.personality_type'), url: personality_type_importers_path

        = render "shared/buttons/download_json_button", url: personality_types_path(format: :json)

  / Filter
  .flex-grow-1
    = form_for "", url: personality_types_path, method: 'get' do |f|
      .d-flex
        = render "shared/filters/filter_name", placeholder: t('shared.search_by_code_or_name')

        .ml-2
          = render "shared/buttons/search_button"
